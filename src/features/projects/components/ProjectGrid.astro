---
import type { Project } from '../queries';
import ProjectCard from './ProjectCard.astro';

interface Props {
  projects: Project[];
}

const { projects } = Astro.props;
---

<ul class="mt-block divide-rule flex flex-col divide-y">
  {projects.map((project) => <ProjectCard project={project} />)}
</ul>
